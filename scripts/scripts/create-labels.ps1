
# Template PowerShell script for creating Sensitivity Labels in Microsoft Purview

Write-Host "Creating CUI Sensitivity Labels..."

# Example (placeholder) commands:
# Connect-IPPSSession  # Uncomment when running in an actual tenant

# CUI General Label
# New-Label -Name "CUI" `
#   -Tooltip "Controlled Unclassified Information" `
#   -EncryptionEnabled $true `
#   -ContentMarkingHeader "CUI" `
#   -ContentMarkingFooter "Handle per CMMC Level 2"

Write-Host Replace placeholders with live tenant settings."

